import { PrismaClient } from '@prisma/client';
// PrismaClient 인스턴스를 생성합니다.
// 이 인스턴스를 통해 애플리케이션의 다른 부분에서 데이터베이스와 상호작용합니다.
const prisma = new PrismaClient();
export default prisma;
//# sourceMappingURL=prisma.js.map